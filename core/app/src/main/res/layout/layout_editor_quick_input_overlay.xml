<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clickable="false"
    android:focusable="false"
    android:clipChildren="false"
    android:clipToPadding="false">

    <!--
         Dedicated shell for the UP expansion surface.
         The leading space preserves the same horizontal relationship as the in-container layout:
         reserved space on the left, card content on the right, and the toggle kept outside the card width.
    -->
    <LinearLayout
        android:id="@+id/overlay_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:clickable="true"
        android:focusable="true"
        android:clipChildren="false"
        android:clipToPadding="false">

        <Space
            android:layout_width="40dp"
            android:layout_height="match_parent"
            android:clickable="false"
            android:focusable="false" />

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            app:cardCornerRadius="24dp"
            app:strokeWidth="0dp"
            app:cardElevation="0dp"
            app:cardBackgroundColor="?attr/colorSurface">

            <!--
                 BlurView stays as a background-only layer here.
                 SymbolInputView must remain a sibling foreground layer, not a BlurView child,
                 otherwise pressed/ripple feedback can disappear on the UP overlay path.
            -->
            <FrameLayout
                android:id="@+id/quick_input_surface"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:clipChildren="true"
                android:clipToPadding="true">

                <eightbitlab.com.blurview.BlurView
                    android:id="@+id/blurView"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:clickable="false"
                    android:focusable="false" />

                <com.tom.rv2ide.ui.SymbolInputView
                    android:id="@+id/symbol_input"
                    android:layout_width="match_parent"
                    android:layout_height="136dp"
                    android:background="@android:color/transparent" />

            </FrameLayout>
        </com.google.android.material.card.MaterialCardView>
    </LinearLayout>
</FrameLayout>
